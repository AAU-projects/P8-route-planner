/// Arguments to pass with namedRoute
class ConfirmScreenArguments {
  /// Default constructor
  ConfirmScreenArguments(this.email, this.type);

  /// The user's email
  final String email;
  /// The type on confirm
  final String type;
}
/// The supported types of SQLite
enum DatabaseTypes {
  /// Signed integer
  INTEGER,
  /// Floating point value
  REAL,
  /// Text string
  TEXT,
  /// Blob of data, stored exactly as it was input
  BLOB
}